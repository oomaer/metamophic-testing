# SMTP Repeated Recipient Metamorphic Relation

## Overview
This test validates RFC 5321 repeated recipient behavior - servers should handle duplicate RCPT TO commands gracefully (accept or reject consistently).

**Metamorphic Relation:**
- Connection A: Send email with single RCPT TO
- Connection B: Send email with same RCPT TO repeated multiple times (2-5 times)
- Expected: Both connections should get identical final response codes

## Files

- `grammar.fan` - Fandango grammar defining the repeated recipient metamorphic relation
- `script.py` - Main test script that runs Fandango and captures violations
- `run_test.py` - Test runner that manages SMTP server lifecycle
- `generate_all_configs.py` - Generates colorized summary report from test logs
- `test_output.log` - Generated log file with detailed test results
- `all_test_cases.txt` - Generated summary report with all test case details

## Usage

### Quick Start (Recommended)
Run the complete test suite with automatic server management:
```bash
python run_test.py
```

This will:
1. Kill any existing test servers
2. Start SMTP servers on ports 8026 and 8027
3. Run the metamorphic tests
4. Display summary with violations/passes
5. Clean up servers automatically

### Generate Summary Report
After running tests, generate a detailed summary:
```bash
python generate_all_configs.py
```

This creates `all_test_cases.txt` with:
- Test case flows (single vs repeated RCPT TO)
- Violation details
- Pass/fail status per test
- Final compliance summary

### Manual Run
If you want to run just the test script (servers must be running):
```bash
python script.py
```

## Output

### Console Output
- Real-time test case execution
- Violation markers: `[VIOLATION]`
- Pass markers: `[PASS]`
- Progress separators
- Summary statistics

### Summary Report Example
```
================================================================================
 SMTP REPEATED RECIPIENT - ALL TEST CASES
================================================================================

Total test cases: 4

Test Case #1
--------------------------------------------------------------------------------
  Connection A: Single RCPT TO
  Connection B: 2 repeated RCPT TO (same address)

  Result: PASS

Test Case #2
--------------------------------------------------------------------------------
  Connection A: Single RCPT TO
  Connection B: 3 repeated RCPT TO (same address)

  Result: PASS

================================================================================
 SUMMARY
================================================================================
Total test cases:  4
Passed:            4
Failed:            0

RFC 5321 Compliance: VERIFIED
```

## Test Results

The script tracks:
- **Total test cases**: Number of solutions generated by Fandango
- **Passed**: Solutions where both single and repeated recipients succeeded
- **Violations**: Solutions where behavior differs between single/repeated

## Mutation Testing

To test with different mutants, edit the `mutant_id` in:
```
/Users/i7949486/Desktop/uds/thesis/mutation/aiosmtpd/run-test-server.py
```

Available mutants:
- `NONE` - Baseline (correct behavior)
- `STRICT_DUP_REJECT` - Rejects duplicate recipients
- `CASE_INSENSITIVE_REJECT` - Case-insensitive duplicate checking
- `MAX_RCPT_LIMIT_2` - Enforces max 2 recipients
- `REVERSE_LOGIC` - Inverted acceptance logic

## Troubleshooting

**Port already in use:**
```bash
pkill -f run-test-server.py
```

**Test hangs:**
- Check if servers are responding: `telnet localhost 8026`
- Increase timeout in `run_test.py` (default: 300s)

**No violations detected:**
- Check grammar constraints in `grammar.fan`
- Verify servers are returning proper SMTP codes
- Review `test_output.log` for detailed execution flow
